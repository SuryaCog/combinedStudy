import { CustomdrDirective } from './customdr.directive';

describe('CustomdrDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdrDirective();
    expect(directive).toBeTruthy();
  });
});
